name: Docker Build

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@/v2
      - name: Build Image
        run: docker build .

