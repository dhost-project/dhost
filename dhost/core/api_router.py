from rest_framework import routers

#from dhost.users.api import viewsets as users_viewsets

router = routers.DefaultRouter()

#router.register(r'user', users_viewsets.UserViewSet)

urlpatterns = router.urls
